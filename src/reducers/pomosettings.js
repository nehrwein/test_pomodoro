import { createSlice } from "@reduxjs/toolkit";

const pomosettings = createSlice({
  name: 'pomosettings',
  initialState: {
    workMinutes: 25,
    breakMinutes: 5
  },
  reducers: {

  }
})

export default pomosettings